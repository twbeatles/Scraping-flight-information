name: Quality

on:
  push:
  pull_request:

jobs:
  verify:
    runs-on: ubuntu-latest
    env:
      QT_QPA_PLATFORM: offscreen

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Check tracked text encoding
        run: python scripts/check_tracked_text.py --check-lf

      - name: Run pyright
        run: pyright --warnings
      # PyQt-based pytest coverage stays local because the hosted Ubuntu runner
      # does not provide the native graphics stack the GUI test bootstrap needs.
